# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# new window default border
new_window pixel 1
new_float pixel 1

focus_follows_mouse no

force_focus_wrapping no

# name the workspaces
# set $tag1 "1: sh"
# set $tag2 "2: vim"
# set $tag3 "3: db"
# set $tag9 "9: im"
# set $tag10 "10: www"

#Back and Forth
workspace_auto_back_and_forth no
bindsym $mod+z move container to workspace back_and_forth; workspace back_and_forth

bindsym $mod+p workspace prev

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier Mod1

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec urxvtc
#bindsym Mod1+Return exec urxvtc -e zsh -c "tmux -q has-session -t local && tmux attach-session -d -t local || tmux new-session -slocal"

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+i exec dmenu_extended_run
# bindsym $mod+i exec dmenu_run

# next/previous workspace
bindsym Mod1+Tab focus right
bindsym $mod+Tab workspace back_and_forth

# Start common apps
# bindsym $mod+Shift+Return exec chromium-browser
bindsym $mod+Shift+Return exec firefox

# Change focused window
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+h focus left

# Move focused window
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right
bindsym $mod+Shift+H move left

# Resize Containers, Vim-style
bindsym $mod+Control+j resize grow height 15 px or 15 ppt
bindsym $mod+Control+k resize shrink height 15 px or 15 ppt
bindsym $mod+Control+l resize grow width 15 px or 15 ppt
bindsym $mod+Control+h resize shrink width 15 px or 15 ppt

# pulse audio volume control
bindsym $mod+Up exec --no-startup-id ~/.i3/audio2.sh raise
bindsym XF86AudioRaiseVolume exec ~/.i3/audio2.sh raise
bindsym $mod+Down exec --no-startup-id ~/.i3/audio2.sh lower
bindsym XF86AudioLowerVolume exec ~/.i3/audio2.sh lower
bindsym $mod+Shift+s exec --no-startup-id ~/.i3/audio2.sh mute
bindsym XF86AudioMute exec ~/.i3/audio2.sh mute

bindsym $mod+Right exec /usr/bin/xbacklight -inc 10
bindsym $mod+Left exec /usr/bin/xbacklight -dec 10
bindsym XF86MonBrightnessUp exec light -A 10 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -U 10 # decrease screen brightness

# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym 113 resize shrink width 10 px or 10 ppt
    bindsym 116 resize grow height 10 px or 10 ppt
    bindsym 111 resize shrink height 10 px or 10 ppt
    bindsym 114 resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# split in horiZontal orientation (Tall)
# need to change this because "Mod+h" no longer free
bindsym $mod+n split h
# split in vertical orientation
bindsym $mod+Shift+n split v

# toggle splitting mode
bindsym $mod+b layout toggle split

# go fullscreen
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+d layout default
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+Shift+c focus parent

# focus the child container
bindsym $mod+c focus child

# switch to urgent workspace
bindsym $mod+u [urgent=latest] focus

# keyboard layouts
bindsym $mod+Control+d exec setxkbmap de
bindsym $mod+Control+u exec setxkbmap us

# scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+o scratchpad show

# switch to next workspace
bindsym $mod+semicolon workspace next

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace 1; workspace 1
bindsym $mod+Shift+at move container to workspace 2; workspace 2
bindsym $mod+Shift+numbersign move container to workspace 3; workspace 3
bindsym $mod+Shift+dollar move container to workspace number 4; workspace 4
bindsym $mod+Shift+percent move container to workspace number 5
bindsym $mod+Shift+asciicircum move container to workspace number 6
bindsym $mod+Shift+ampersand move container to workspace number 7
bindsym $mod+Shift+asterisk move container to workspace 8; workspace 8
bindsym $mod+Shift+parenleft move container to workspace 9; workspace 9
bindsym $mod+Shift+parenright move container to workspace 10; workspace 10

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

###  ADDED KEY BINDINGS  ###
bindsym $MOD+SHIFT+f exec thunar
bindsym $MOD+g exec pomodoneapp
bindsym $MOD+SHIFT+o exec urxvt -e 'ranger'
bindsym $MOD+SHIFT+m exec mysql-workbench
# bindsym $MOD+SHIFT+g exec gvim
bindsym $MOD+a exec gnome-do
bindsym $mod+Shift+p exec sh ~/.i3/i3lock.sh
bindsym Print exec scrot '%Y-%m-%d-%T_$wx$h_scrot.png' -e 'mv $f ~/Pictures/screenshots/'

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#     font pango:DejaVu Sans Mono 10
#     colors {
#         # Whole color settings
#         background #3b4252
#         statusline #d8dee9
#         separator  #4c566a

#         # Type             border  background font
#         focused_workspace  #7bf473 #5e81ac #d8dee9
#         active_workspace   #333333 #434c5e #d8dee9
#         inactive_workspace #333333 #2e3440 #888888
#         urgent_workspace   #aa0000 #990000 #d8dee9
#     }

#     position top
#     status_command i3status -c ~/.i3/i3status.conf
# }

# changed these colours for readability
# class border backgr. text
client.focused #a84a28 #ce7e61 #ffffff
client.unfocused #333333 #333333 #ffffff

# workspace targeting
assign [class="Chromium"] 10
assign [class="Firefox"] 10

# workspace 1 output DP-1
# workspace 9 output DP-1
# # workspace 3 output HDMI1
# workspace 10 output DP-1
# workspace 2 output HDMI1

# default floating applications
for_window [class="Xfce4-notifyd"] border none
for_window [class="Xfce4-notifyd"] floating enable
for_window [class="PomoDoneApp"] floating enable
for_window [class="Gnome-calculator"] floating enable

exec --no-startup-id xsetroot -solid "#333333"
# disable screensaver
exec xset -dpms; xset s off

exec --no-startup-id nm-applet
exec --no-startup-id pasystray

exec --no-startup-id sh ~/.i3/startup.sh

#######
#THEME#
#######

# set primary gruvbox colorscheme colors
set $bg #282828
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $darkgray #1d2021

# font used by i3 for titles and bars
font pango:Hack 9

#start of bar section
bar {
    position top
    colors {
        # bar background color
        background $bg
        # text color used for blocks that do not have a color specified.
        statusline $gray
        # workspaces section
        #                    border  backgr. text
        focused_workspace    $aqua $aqua $darkgray
        inactive_workspace   $darkgray $darkgray $yellow
        active_workspace     $darkgray $darkgray $yellow
        urgent_workspace     $red $red $bg
    }
    status_command i3status -c ~/.i3/i3status.conf
}
#end of bar section

#start of window title bars & borders section

# green gruvbox
# class                 border|backgr|text|indicator|child_border
#client.focused          $green $green $darkgray $purple $darkgray
#client.focused_inactive $darkgray $darkgray $yellow $purple $darkgray
#client.unfocused        $darkgray $darkgray $yellow $purple $darkgray
#client.urgent           $red $red $white $red $red

# blue gruvbox
# class                 border|backgr|text|indicator|child_border
client.focused          $blue $blue $darkgray $purple $darkgray
client.focused #a84a28 #ce7e61 #ffffff
client.focused_inactive $darkgray $darkgray $yellow $purple $darkgray
client.unfocused        $darkgray $darkgray $yellow $purple $darkgray
client.urgent           $red $red $white $red $red

#end of window title bars & borders section
