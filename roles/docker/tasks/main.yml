---

- name: install docker
  apt:
    state: latest
    pkg:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin

- name: install docker-compose
  shell: "pip install --user --upgrade docker-compose"
