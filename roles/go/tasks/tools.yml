---

- name: install benchstat
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u golang.org/x/perf/cmd/benchstat executable=/bin/zsh

- name: install goweight
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u github.com/jondot/goweight executable=/bin/zsh

- name: install gobin
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u github.com/rjeczalik/bin/cmd/gobin executable=/bin/zsh

- name: install gomove
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u github.com/ksubedi/gomove executable=/bin/zsh

- name: install iferr
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u github.com/koron/iferr executable=/bin/zsh

- name: install impl
  shell: >
    source ~/.config/zsh/.zshrc &&
    go get -u github.com/josharian/impl executable=/bin/zsh
