# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# new window default border
#new_window 1pixel
new_window pixel 2
new_float pixel 2

focus_follows_mouse no

force_focus_wrapping no

# name the workspaces
set $tag1 "1: term"
set $tag2 "2: code"
set $tag3 "3: db"
set $tag8 "8: daemons"
set $tag9 "9: im"
set $tag10 "10: web"

#Back and Forth
workspace_auto_back_and_forth yes
bindsym $mod+z move container to workspace back_and_forth; workspace back_and_forth

bindsym $mod+p workspace prev

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier Mod1

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec xfce4-terminal

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+i exec dmenu_run

# next/previous workspace
bindsym Mod1+Tab focus right
#bindsym Mod1+Shift+Tab focus left
bindsym $mod+Tab workspace back_and_forth

# Start common apps
bindsym $mod+Shift+Return exec chromium-browser
#bindsym $mod+Shift+T exec thunderbird; workspace $tag3

# Change focused window
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+h focus left

# Move focused window
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right
bindsym $mod+Shift+H move left

# Resize Containers, Vim-style
bindsym $mod+Control+j resize grow height 3 px or 3 ppt
bindsym $mod+Control+k resize shrink height 3 px or 3 ppt
bindsym $mod+Control+h resize grow width 3 px or 3 ppt
bindsym $mod+Control+l resize shrink width 3 px or 3 ppt

# pulse audio volume control
set $sink `pactl list short sinks | grep RUNNING | cut -f1`
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume $sink -- +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume $sink -- -5%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute $sink toggle

bindsym XF86MonBrightnessUp exec /usr/bin/xbacklight -inc 10
bindsym XF86MonBrightnessDown exec /usr/bin/xbacklight -dec 5

# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode
    bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
#bindsym uring resize grow width 10 px or 10 ppt

# same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# split in horiZontal orientation (Tall)
# need to change this because "Mod+h" no longer free
bindsym $mod+n split h
# split in vertical orientation
bindsym $mod+Shift+n split v

# toggle splitting mode
bindsym $mod+b layout toggle split

# go fullscreen
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+d layout default
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+Shift+c focus parent

# focus the child container
bindsym $mod+c focus child

# switch to urgent workspace
bindsym $mod+u [urgent=latest] focus

# keyboard layouts
bindsym $mod+Control+d exec setxkbmap de
bindsym $mod+Control+u exec setxkbmap us

# scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+o scratchpad show

# switch to next workspace
bindsym $mod+semicolon workspace next

# switch to workspace
bindsym $mod+1 workspace $tag1
bindsym $mod+2 workspace $tag2
bindsym $mod+3 workspace $tag3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace $tag8
bindsym $mod+9 workspace $tag9
bindsym $mod+0 workspace $tag10

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace $tag1; workspace $tag1
bindsym $mod+Shift+at move container to workspace $tag2; workspace $tag2
bindsym $mod+Shift+numbersign move container to workspace $tag3; workspace $tag3
bindsym $mod+Shift+dollar move container to workspace number 4; workspace 4
bindsym $mod+Shift+percent move container to workspace number 5
bindsym $mod+Shift+asciicircum move container to workspace number 6
bindsym $mod+Shift+ampersand move container to workspace number 7
bindsym $mod+Shift+asterisk move container to workspace $tag8; workspace $tag8
bindsym $mod+Shift+parenleft move container to workspace $tag9; workspace $tag9
bindsym $mod+Shift+parenright move container to workspace $tag10; workspace $tag10

# reload the configuration file
#bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

###  ADDED KEY BINDINGS  ###
bindsym $MOD+SHIFT+f exec thunar
bindsym $MOD+SHIFT+m exec mysql-workbench
bindsym $MOD+SHIFT+g exec gvim
bindsym $MOD+a exec gnome-do
bindsym $mod+Shift+P exec i3lock -i /home/jmollowitz/Downloads/get_away_from_my_computer-wallpaper-1920x1080.png



# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {

# font for window titles. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
    font pango:DejaVu Sans Mono 10
        colors {


        # Whole color settings
        background #000000
        statusline #ffffff
        separator  #666666

        # Type             border  background font
        focused_workspace  #008fff #007fff #ffffff
        active_workspace   #333333 #5f676a #ffffff
        inactive_workspace #333333 #222222 #888888
        urgent_workspace   #aa0000 #990000 #ffffff




#            statuslin	#76c2d6
#            background	#191919
#            focused_workspace      #191919 		#191919 	#bf3f34
#            active_workspace          #191919 		 #191919 	#696f89
#            inactive_workspace     #191919 		#191919 	#696f89
#            urgent_workspace        #191919 		#191919 	#c7a551




#background #666666
#statusline #ffffff

#focused_workspace  #ffffff #285577
#active_workspace   #ffffff #333333
#inactive_workspace #888888 #222222
#urgent_workspace   #ffffff #900000
        }
    #status_command i3status
    position top
    status_command i3status -c ~/.i3/i3status.conf
}

# changed these colours for readability
# class border backgr. text
client.focused #4c7899 #CA226B #ffffff
client.unfocused #333333 #747170 #ffffff


# workspace targeting
assign [class="Term"] $tag1
assign [class="Gvim"] $tag2
assign [class="Mysql-workbench-bin"] $tag3
assign [class="Pidgin"] $tag9
assign [class="Hipchat"] $tag9
assign [class="Skype"] $tag9
assign [class="Thunderbird"] $tag9
assign [class="Chrome"] $tag10

# default floating applications
for_window [class="Xfce4-notifyd"] floating enable
for_window [class="Xfce4-notifyd"] border none

# as i use a laptop with trackpoint and the touchpad disturbs me while typing, disable it
#exec synclient TouchpadOff=1
# pulse audio system tray
exec pasystray
# disable screensaver
exec xset -dpms; xset s off



exec /usr/libexec/gnome-settings-daemon
exec --no-startup-id nm-applet
#exec --no-startup-id gnome-settings-daemon

exec --no-startup-id feh --bg-fill /home/jmollowitz/Downloads/newkidsturbo.jpg

# autostart apps on specific workspaces
#exec --no-startup-id i3-msg '$tag1; exec /usr/bin/xfce4-terminal;exec /usr/bin/xfce4-terminal'
#exec --no-startup-id i3-msg '$tag2; exec /usr/bin/gvim'
#exec --no-startup-id i3-msg '$tag3; exec /usr/bin/chromium-browser'
#exec --no-startup-id i3-msg '$tag10; exec /usr/bin/pidgin'
